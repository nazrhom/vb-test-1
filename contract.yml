type: sw.supervisor
name: Resinio supervisor
version: '1.0.0'
aliases: []
tags: []
data: {}
variants:
  - slug: '{{this.data.arch}}-supervisor'
    data:
      docker_tag: 'v{{this.componentVersion}}'
    variants:
      - data:
          arch: amd64
      - data:
          arch: armv7hf
      - data:
          arch: rpi
      - data:
          arch: aarch64
      - data:
          arch: i386
      - data:
          arch: i386-nlp